
#
# x <- rnorm(1e7)
#
#
# system.time(FastDesc(x, p=F))
# system.time(Desc(x, p=F))
#
# FastDesc(d.pizza, wrd=wrd)
#
#
#
# library(DescTools)
# wrd <- GetNewWrd()
#
# FastDesc(d.pizza[,c("temperature","weekday","driver","count","rabate","date")], wrd=wrd)
#
# Desc(temperature ~ delivery_min, data=d.pizza, digits=1, wrd=wrd)
# Desc(temperature ~ driver, data=d.pizza, digits=1, wrd=wrd)
# Desc(driver ~ temperature, data=d.pizza, digits=1, wrd=wrd)
#
# Desc(operator ~ driver, data=d.pizza, digits=1, wrd=wrd)


